{
  "name": "@artifact/face-test",
  "exports": "./main.ts",
  "tasks": {
    "watch": "deno test -A --watch",
    "test": "deno test -A",
    "lint": "deno lint --fix",
    "fmt": "deno fmt",
    "ok": "deno check && deno task test && deno fmt --check && deno lint"
  }
}

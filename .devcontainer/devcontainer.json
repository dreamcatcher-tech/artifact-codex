{
  "name": "Codex",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },

  "mounts": [
    "source=${localEnv:HOME}/.codex,target=/tmp/host-codex,type=bind,consistency=cached,readonly",
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],

  "remoteUser": "ubuntu",
  "customizations": {
    "vscode": {
      "settings": {
        "deno.enable": true,
        "javascript.validate.enable": false,
        "typescript.validate.enable": false
      },
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "denoland.vscode-deno",
        "bierner.markdown-mermaid"
      ]
    }
  },
  "containerEnv": {
    "DOCKER_HOST": "unix:///var/run/docker.sock"
  },
  "remoteEnv": {
    "SSH_AUTH_SOCK": "${localEnv:SSH_AUTH_SOCK}"
  },
  "postCreateCommand": "bash .devcontainer/postCreate.sh postCreate",
  "postStartCommand": "bash .devcontainer/postCreate.sh postStart"
}

{
  "name": "@artifact/mcp-agents",
  "exports": "./main.ts",
  "tasks": {
    "watch": "deno test -A --trace-leaks --watch",
    "test": "deno test -A",
    "inspect": "npx -y @modelcontextprotocol/inspector main.ts",
    "lint": "deno lint --fix",
    "fmt": "deno fmt",
    "ok": "deno check && deno task test && deno fmt --check && deno lint"
  }
}

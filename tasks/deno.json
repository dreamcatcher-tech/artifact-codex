{
  "name": "@artifact/tasks",
  "exports": "./mod.ts",
  "tasks": {
    "nfs-mount": "deno run -A ./nfs_mount.task.ts",
    "self-mount-check": "deno run -A ./self_mount_check.task.ts",
    "test": "deno test -A",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "ok": "deno fmt --check && deno lint && deno test -A"
  }
}

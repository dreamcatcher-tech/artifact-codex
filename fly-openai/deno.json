{
  "tasks": {
    "dev": "deno run --allow-net --allow-env main.ts",
    "test": "deno test -A ",
    "lint": "deno lint --fix",
    "fmt": "deno fmt",
    "ok": "deno check main.ts && deno task test && deno fmt --check && deno lint"
  }
}
